[package]
name = "operai-build"
version = "0.1.0"
edition = "2024"
license-file.workspace = true
description = "Build-time utilities for Operai tools"

[dependencies]
# No runtime dependencies needed for the build logic as it relies on std
